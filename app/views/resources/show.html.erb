<h1>Resources</h1>
<p>Below are a list of resources that will help you learn to code</p>


<div class="table">
<table>
  <tr>
    <td><%= @resource.name %></td> 
    <td><%= @resource.description %></td> 
    <td><%= @resource.url %></td>
    <td><%= @resource.content %></td>
  </tr>
</table>
</div>

<p>
  <a href="/resources/<%= @resource.id %>/edit">Edit</a>
  <%=  link_to 'Delete', @resource, :method => 'delete' %>
</p>